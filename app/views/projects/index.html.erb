<h1>Listing projects</h1>

<table>
  <tr>
  </tr>

<% for projects in @projects %>
  <tr>
    <td><%= link_to 'Show', projects %></td>
    <td><%= link_to 'Edit', edit_projects_path(projects) %></td>
    <td><%= link_to 'Destroy', projects, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New projects', new_projects_path %>
