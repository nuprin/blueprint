<%= javascript_include_tag 'tasks/index' %>
<%= stylesheet_link_tag 'tasks/index' %>

<% @users.each do |user| %>
<% next if user.task_list.empty? %>
<script type="text/javascript">
$(function () {
  Tasks.makeSortable("project_<%= user.id %>_tasks");
});
</script>
<table id="project_<%= user.id %>_tasks" class="task_list">
  <thead>
    <tr>
      <th colspan="5"><%= h user.name %></th>
    </tr>
  </thead>
  <!--tfoot>
    <tr>
      <td colspan="5" class="add_task">Add task</td>
    </tr>
  </tfoot-->
  <tbody>
    <% user.task_list.each do |list_item| %>
    <% task = list_item.task %>
    <tr id="list_item_<%= list_item.id %>">
      <td class="drag_handle">
        <% if task.assignee == viewer %>
          <%= check_box_tag "complete_#{list_item.id}", '' %>
        <% else %>
          <%= image_tag 'drag_handle.gif' %>
        <% end %>
      </td>
      <td class="task_name"><%= link_to h(task.title), task_path(task) %></td>
      <td class="task_project"><%= h task.project.title %></td>
      <td class="task_estimate"><%= task.estimate %></td>
      <td class="task_due"><%=
        task.due_date.strftime("%b %e") if task.due_date %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<% end %>
